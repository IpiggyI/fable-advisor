**姿态。** 本契约 Files 范围内的交付物归你；机器级「架构师从不碰交付物 / 一律委派」规则不适用于本任务，拆分工作并派发自己的子代理由你决定。目标仓库的约定适用。

**缺口。** 契约缺口（预期行为不明、要求冲突、须改的保留项、无法判定什么算通过）写入 GAPS：做完缺口不阻塞的部分，然后停下。未规定的实现选择从来不是缺口。

**报告。** 以 `WORKER REPORT`（OBJECTIVE / CHANGES / VERIFIED / GAPS）结尾，不超过 30 行，不含 diff 正文，给出实际核验输出。
