# Upstream Sync Digest — 2026-07-26（report-only，未同步）

Source: `upstream/main`（上次同步：2026-07-11 → `c5a1bf5`）
Range: 1 个新提交 `2cf102d`，tag **v4.0.0**，8 files, +98/−146

## 💥 上游 v4.0.0 —— 单提交但属破坏性重构

> "Opus orchestrates, Codex is the routine lane, Fable escalates and reviews"

1. **会话架构师 Fable → Opus**：Fable 只留给最难的一次性实现与终审。
2. **codex-implementer（wrapper agent）成为默认打字车道**（上游仍走 agent 包装，非 runner）。
3. **新增 `agents/fable-implementer.md`**：Fable 5 高复杂度实现车道——spec 无法承载判断力的任务（并发、算法、安全路径、难调试、大爆炸半径重构），或 routine lane 同一任务失败两次后升级（"重复失败=分类错误"）。
4. **fable-advisor 增加强制交付终审**：每个 deliverable 完成前必过一次 fresh-eyes 评审，ship / fix-first / rethink 三态裁决；并诚实注明同模型评审的局限。
5. **grok-implementer 删除**，Grok CLI 不再是依赖。

## 🔥 与本 fork 的冲突面（若整体 rebase）

- `skills/orchestration/SKILL.md`：**正面结构冲突**——上游重写的车道表/判定规则，恰是我们今天（ADR 0006）改成两段式帕累托的同一段落。
- 车道结构互为镜像冲突：上游删 grok 车道（我们保留且在用户 profile 里有排名）；上游保留 codex wrapper agent（我们按 ADR 0002 已删除，改为 runner + receipt gate）。
- 本地 9 个 fork-only 提交 + 今日未提交的 Pareto 改造全部压在冲突面上。

**结论：整体 rebase 不可行，只能选择性吸收（cherry-pick 文件/hunk 级）。**

## ✅ 建议吸收

| 项 | 价值 | 吸收方式 |
|---|---|---|
| `agents/fable-implementer.md`（新文件，零冲突） | 补齐我们帕累托模型能力轴的最高档：In-house(Opus) 之上再加 Fable 档实现车道，恰好与 ADR 0006 的胜任集合模型互补 | 新文件直接引入，车道表加一行，用户 profile 补该车道的速度/价格/能力条目 |
| "routine lane 失败两次 → 分类错误，升级车道"一句 | 与我们"同一问题两次尝试失败→咨询 advisor"同构，补上实现侧的升级触发器 | SKILL.md 判定规则一句话 |
| fable-advisor 终审角色（agent diff + doctrine 措辞） | 把我们已有的"交付前咨询一次"从建议强化为协议，含同模型评审局限的诚实披露 | 可选：是否"强制"由用户定 |

## ❌ 建议不吸收

- **grok 车道删除**——用户 profile（2026-07）仍为 grok 保留速度/价格最优排名；机器级前提是"grok 未认证"而非"grok 无价值"，前提未落地。
- **codex wrapper agent 回归**——与 ADR 0002（dewrapper + receipt gate）正面冲突，我们的 runner 方案有验收凭证机制，上游 wrapper 没有。
- **架构师钉死 Opus**——我们的模式规则已同时接受 Fable/Opus 系架构师，比上游更宽，无需收窄。

## 📌 前置条件

任何吸收操作前，先提交本地未提交的 ADR 0006 改造（5 文件 + 新 ADR + .scratch spec），否则 cherry-pick 无干净基线。

## 裁决（2026-07-26，用户拍板）

- 第 1 项（fable-implementer 车道）：**不吸收**——太贵且实际使用频率低；另注：上游连续两步（全面引入 Fable 档 + 删除最便宜的 grok 车道）显示作者正朝该工具设立初衷（成本套利）的反方向演进，进一步支持本 fork 按 ADR 0001 独立取舍。
- 第 2 项（失败两次→重分类升级）：**已吸收**，措辞适配两段式模型（升级=重跑 stage 1，非固定升级目标）。
- 第 3 项（advisor 强制终审）：**暂不吸收**，维持"交付前咨询一次"的建议边界；可复盘。
