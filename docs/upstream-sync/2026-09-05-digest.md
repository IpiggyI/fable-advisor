# Upstream Sync Digest — 2026-09-05（report-only，未同步）

Source: `upstream/main`（上次整本同步：2026-07-11 → `c5a1bf5`）
Range: `main` ← 6 个上游提交，23 个 fork-only 提交
Local: **3.11.0** `87a16cd`（已与 `origin/main` 对齐；cache 同为 3.11.0）
Upstream HEAD: **v5.0.0-2-g4d6cc62**（tag `v5.0.0` = `f8ba235`，再加 2 条 README）

前置：工作区有未提交改动（`AGENTS.md`、`CONTEXT.md`、`docs/agents/cursor-lane-gate.md`、`tests/test_lane_family_gate.py`、未跟踪 `cursor-hooks/fable-lane-pin.mdc`）。本次只写摘要，不 rebase、不吸收。

## 上游 6 个提交

| SHA | 日期 | 内容 |
|---|---|---|
| `2cf102d` | 2026-07-25 | **v4.0.0** — Opus 当架构师；Codex wrapper 当日常打字；新增 `fable-implementer`；删 Grok；advisor 强制终审 |
| `3088622` | 2026-08-04 | 日常车道钉到 GPT-5.6 Luna、effort=`max` |
| `ad2bdc3` | 2026-08-04 | wrapper 声明退出机器级 `~/.codex/AGENTS.md` 编排默认；空 diff 不得报 complete |
| `f8ba235` | 2026-09-02 | **v5.0.0** — Fable 5.1 当架构师；Luna 日常 / Sol 难活；每任务自选用力；六段 spec；Codex 插件 |
| `ed4b568` | 2026-09-02 | README 文案 |
| `4d6cc62` | 2026-09-02 | README 加 30 秒演示视频与海报 |

三方差异（merge-base `c5a1bf5` → `upstream/main`）：10 files, +269/−163（含演示二进制）。

## 🔥 冲突面（若整本 rebase）

- 真重叠：`.claude-plugin/marketplace.json`、`README.md`
- 本地已搬走（rebase 会在根目录加回）：`.claude-plugin/plugin.json`、`agents/codex-implementer.md`、`agents/fable-advisor.md`、`skills/orchestration/SKILL.md`
- 上游新增：`agents/sol-implementer.md`、`assets/fable-advisor-demo.{png,mp4}`

结构冲突与 2026-07-26 相同，且更深：上游仍是根目录 wrapper agent；本 fork 是 `plugin/` + runner + receipt gate + Cursor 双 harness。**整本 rebase 不可行。**

## 📌 本 fork 已有（不必再抄）

- 每任务 `model`/`effort`：JSON spec + fail-loud 白名单（[ADR 0003](../../docs/adr/0003-codex-lane-param-policy.md)）。不是上游那种第六行 Markdown。
- Luna / Sol / Terra：同一个 `run-codex.mjs` 的 `model` 字段。不是两个 wrapper。
- 失败两次 → 重跑 stage 1（`plugin/skills/orchestration/SKILL.md:34`）。2026-07-26 已吸收。
- 多步交付结束前咨询一次 advisor（同文件 `:88`），并写了同模型评审的局限（`:90`）。
- Codex 对抗评审：**不是已落地的能力。** 只有 `lanes-claude-code.md:41` 一句指针（ADR 0009 把它收编为第三档「可选 reviewer」）。主文 `SKILL.md` 第三档只写「上下文干净的 reviewer 子代理」，不点名该命令。Cursor 车道文件零提及。本机 `codex@openai-codex` 为 `false`，且该 slash 命令带 `disable-model-invocation: true`（须用户手打，架构师不能自己调用）。上一版把这句写成「已经有了」，过满。

## ✅ 值得吸收（fork 形状，稍后；不 rebase）

判定风险：`run-codex.mjs:555-561` 与 `run-grok.mjs:538-545` 在 verification 全 0 时标 `complete`，即使 `changed_files` 是 `[]`。receipt gate 只认 `error_class == complete`（`receipt-gate.py:48`），runner 随后删 pending。静默空跑会被当成做完。

| 项 | 来源 | 吸收方式 | 不要照抄的部分 |
|---|---|---|---|
| 空 diff 不得 complete | `ad2bdc3` | 仅当 `spec.files.length > 0` 且 `changed_files` 为空时，两条 runner 都标新 `error_class`（建议 `no_diff`，不要复用 `verification_failed`）。`files: []` 仍合法（`normalizeSpec` 允许空数组）。副作用：`collectChangedFiles` 在 `git status` 失败时现返回 `[]`，修完后会改为大声失败，不再假 complete。 | 上游 wrapper 的 `STATUS: refused` 报告契约；本 fork 没有 wrapper。 |
| 声明退出机器级编排默认 | `ad2bdc3` | 写入 `run-codex.mjs` 的 `renderPrompt` 前言。范围只覆盖机器级默认，不覆盖目标仓库约定（约定仍走 Constraints）。grok 侧无 `~/.codex/AGENTS.md`，可选镜像一句。 | 把 `~/.codex/config.toml` 一并关掉。 |

第一方探测（2026-09-05，`codex-cli 0.153.4`）：`codex exec` 无单独跳过 `AGENTS.md` 的 flag。`--ignore-user-config` 只跳过 `$CODEX_HOME/config.toml`；`--ignore-rules` 只跳过 execpolicy。前言仍是目前能落地的吸收面。若日后出现机械开关，用开关替换前言。

## ❌ 不吸收

- **删 Grok / 架构师钉死 Fable 5.1 / 恢复 wrapper / 从 `./` 打包** — 与 [ADR 0001](../../docs/adr/0001-upstream-sync-fork.md)、[0002](../../docs/adr/0002-codex-lane-dewrapper-receipt-gate.md)、[0006](../../docs/adr/0006-pareto-lane-routing-inhouse-promotion.md)、[0009](../../docs/adr/0009-grok-lane-dewrapper-runner.md) 和 `docs/agents/plugin-release.md` 正面冲突。上游在撤回本 fork 的存在理由。
- **强制交付终审** — 2026-07-26 用户已暂缓；本 fork 保持「多步结束前咨询一次」。Fable 5 额度已耗尽，「每次必审」会卡住收工。
- **Sol `ultra`** — [ADR 0003](../../docs/adr/0003-codex-lane-param-policy.md) 白名单是 `{low,medium,high,xhigh,max}`。要加，须先用当前 `codex exec` 真实验证 Sol 接受该档（与当时验证 `max` 同一标准）。
- **Codex 插件 `/codex:rescue` 与 stop-time review-gate** — ADR 0009 已拒绝用插件替换 runner。rescue 是另一套 job 架构。
- **演示视频/海报** — 不要打进 `plugin/`；本仓库 README 也没有对应 30 秒 lite 路径。

## 建议

不 rebase。不整本吸收 v4/v5。工作区干净之后，只做上表两行 runner 加固。

fable-advisor 咨询（2026-09-05）：同意上述取舍。决定性风险是空 diff 假 complete。
