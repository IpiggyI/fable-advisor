# fable-advisor

本仓库的公共词汇表：钉死最易记混的术语，供编排与决策散文统一取词。只定义"是什么"，不承载实现细节。

## Language

### 护栏与门禁

**receipt gate**:
Stop hook，fail open；针对的是**主会话自己**——排了 spec 却不跑、或把非-complete receipt 当完成的威胁。
_Avoid_: 笼统称"护栏/guardrail"而不指明针对主会话；勿与历史上的 spawn 护栏混同（后者针对 wrapper 子代理，已随 wrapper 一并退役，见 ADR 0009）。

**决策类型门**:
对任何主代理、任一姿态都适用的一张关键点清单（架构 / 数据迁移 / API 形状 / 重构策略；推翻既定方案；改公共接口或跨模块依赖；放宽验收标准；同一问题两次失败；宣告多步交付物完成前），到点位咨询 advisor。是 doctrine 规则，不是 hook。
_Avoid_: 沿用旧名"承诺边界"；与 receipt gate / lane family gate 这类机械门禁混称；理解成逐 diff 过审或按模型身份触发。

**lane family gate**:
Cursor 用户级 `preToolUse` 门：`Task` 派发具名 agent `fable-advisor` 时必须带显式、非 inherit 的 `model`；守的是"静默继承会话模型"，不查家族。
_Avoid_: 与 receipt gate 混称；称作插件 hook 或本仓 project hook；把用户级 Task pin 规则当成另一套门禁；把它扩到 `generalPurpose` 派发（无标记可区分 worker 与普通 scout）。

### 角色与档位

**角色（role）**:
一次派发的契约形状——接什么输入、有什么权限、回什么产物。只有三个：explorer、worker、advisor。角色不含模型型号。
_Avoid_: 按模型强弱划角色（"高级实现"不是角色，是 worker 的一个档位）；把角色名与车道名混用。

**explorer**:
只读检索角色：在给定范围内收集证据（`file:line`、符号、原文），回报告，不改文件。
_Avoid_: 让它做任何写操作；把"没找到"当成"不存在"。

**worker**:
可写实现角色：在契约的 Files 范围内持有交付物，回 diff 与验证证据。接管卡住任务的是 worker 的高档位加一份接管契约，不是另一个角色。
_Avoid_: 按改动难度另起角色名（机械修改 / 常规实现 / 高级实现都是 worker）；把它当只读。

**advisor**:
只读判断角色，两种请求形状：**决策**（动工前：决定、约束、备选 → verdict）与 **验收**（完工后：契约 + diff + receipt → 是否满足验收标准）。权威来自它读到的代码，不来自它跑的模型。
_Avoid_: 另设"审查员"角色（验收就是 advisor 的一种请求形状）；让它改文件；把它当成实现车道。

**档位（tier）**:
一次派发的能力等级：light / standard / senior。与角色正交——同一个角色可派任一档位。
_Avoid_: 把档位写成型号名；把档位固定绑到某个角色。

**拨盘（dial）**:
车道内的具体填充：型号 + effort。拨盘是档位的实现，不是档位本身。
_Avoid_: 在车道之间比较拨盘（车道级比较按各车道默认拨盘计价）。

**填充表**:
（角色, 档位）→ 候选车道与拨盘的有序表，逐行带日期戳与失效条件；属用户判断，放用户级规则，不入库。
_Avoid_: 把型号排名写进仓库 doctrine；把填充表当成胜任性筛选（它只在胜任集合内做选择）。

### 车道

**lane（车道）**:
到达一个供应商的机制：grok lane、codex lane、claude lane、handoff lane。车道回答"怎么到达"，角色与档位回答"派什么"。Handoff 是条件成员，仅在用户声明后进入候选。
_Avoid_: 用车道名表达路由时机（旧名 Routine / Cross-vendor / In-house 已退役）；把车道名与具体型号混为一谈。

**grok lane**:
经 grok runner 到达 Grok 家族的车道。
_Avoid_: 称它"默认车道"或 "Routine lane"——默认由填充表给，不由车道名给。

**codex lane**:
经 codex runner 到达 GPT 家族的车道。
_Avoid_: 称它 "Cross-vendor lane"；把"跨厂"理解成任意备用路径。

**claude lane**:
以 Claude 子代理到达 Claude 家族的车道，仓内自含、无外部 CLI 依赖。**同模派发**是它的一个拨盘：模型钉为会话模型而非默认别名槽位，只用于编排姿态下的准则散文类交付物。
_Avoid_: 称它 "In-house lane" 或 "Fallback"；用型号名（Opus/Sonnet）指称该车道；把同模派发当成第五条车道。

**handoff lane（交接车道）**:
用户中介的车道：架构师把五部 spec 与操作指南写入 `.fable-advisor/handoff/<slug>.md`，由用户手动带到自己选定的 harness 执行，回来后架构师读 diff、亲自重跑 verification 验收；无机械门禁（不在 receipt gate 视野内）、fail open。
_Avoid_: 未经用户声明将其纳入路由候选；因"免费"把小任务也交接出去；把回传报告当验收证据（证据是 diff）。

### 姿态

**姿态（posture）**:
主代理对交付物的关系，只有两种：编排、实现。两者只差一条规则——是否亲手改交付物；派发任一角色在两种姿态下都可用。选择器是用户声明或上层指示，不是模型身份。
_Avoid_: 用模型身份推断姿态（旧词 `架构师层` / `advisor-only` 已退役）；把"实现姿态"读成"不派子代理"。

**编排姿态**:
主代理不亲手改交付物：写交付契约、派 worker、验收。没人声明时，存在上游任务件即为此姿态。
_Avoid_: 把"写契约"理解成替 worker 写实现步骤；改称组长 / 老师 / 管理者。

**实现姿态**:
主代理可亲手改交付物，同时按需派任一角色；在决策类型门的点位咨询 advisor。没人声明且无上游任务件时为此姿态。
_Avoid_: 读成"一把梭、不派活"；把它当成弱模型专属。

**架构师**:
编排姿态下主代理承担的职责名：持有需求、分解、契约、路由、验收。是职责，不是模式，也不是模型档位。
_Avoid_: 用具体型号名指称；把"架构师"当成任意会写代码的会话。

**主代理**:
用户直接对话的那个会话线程，任一姿态、任一模型。
_Avoid_: 与"架构师"等同（架构师只是它在编排姿态下的职责名）。

### 交付契约与产物

**交付契约（spec）**:
派给车道的五部任务书（Objective / Files / Interfaces / Constraints / Verification）：只规定做什么、授权范围、保留的接口与约束、怎样证明完成；Constraints 未规定的实现选择默认归车道。
_Avoid_: 写成施工说明或步骤教程；把"没写怎么做"当成契约不完整；把 Files 当成穷尽的改动清单而不是授权范围。

**契约缺口**:
交付契约里车道无法自行解决的空洞：预期行为不明、两条要求冲突、必须改动保留接口、无法判定什么算通过。车道上报，架构师修正契约。
_Avoid_: 与"未规定的实现选择"混称；用"use your judgment"回应真正的缺口。

**未规定的实现选择**:
契约边界内、Constraints 没有钉死的局部决定：内部函数划分、等价数据结构、测试组织、契约范围内的错误处理。车道自行决定，不上报、不等待。
_Avoid_: 当成契约缺口退回架构师；拿它当改动保留接口或扩大范围的理由。

**返工票**:
评审未过后回到原车道的派发：只写违反的要求、可复现的问题、期望行为、验证方式，不写修复方案。
_Avoid_: 架构师亲手修改后宣布通过；因结构偏好而非契约违反开返工票。

**交付物**:
随插件发布、被测试、或对外描述行为的产物。编排姿态下一律经 worker 修改，主代理不亲手改；实现姿态下主代理可直接改。
_Avoid_: 在编排姿态下按改动大小判断能否亲手改；把"只是补一个测试 / 修个拼写"当作例外。

**协调件**:
主代理用来组织落地的记录：任务件（issue、spec）、决策记录（ADR、词表）、工作流状态文件、发布版本字段。任一姿态下主代理可直接写。
_Avoid_: 把协调件当成交付物走车道；借协调件之名修改交付物。

### 模型别名

**别名槽位**:
`opus` / `sonnet` / `haiku` / `fable` 这类模型别名是可被环境变量重定向的**指针**，不是模型身份。
_Avoid_: "跑在 Opus 槽位 = 跑的是 Opus"这类说法；把别名名当成已解析的具体型号。
